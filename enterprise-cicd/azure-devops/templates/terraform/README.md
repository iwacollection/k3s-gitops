# Terraform Templates

Reusable Azure DevOps templates for fmt, validate, plan, policy checks, protected apply, verification and drift checks.

Plan and Apply must use separate identities. Apply templates must target protected Azure DevOps Environments and never embed long-lived cloud secrets.
