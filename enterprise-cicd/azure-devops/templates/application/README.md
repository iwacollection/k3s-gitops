# Application Templates

Azure DevOps adapters for application CI when a workload uses Azure Repos/Azure Pipelines.

Language-specific build behavior must delegate to shared scripts/build images so GitHub Actions and Azure Pipelines do not drift.
