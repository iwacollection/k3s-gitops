# Platform Pipelines

Azure DevOps entry pipelines for platform Terraform stacks such as governance, connectivity, identity, ACR and AKS. Entry YAML composes required enterprise templates rather than embedding implementation logic.
