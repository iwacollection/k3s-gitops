# platform/aks stack

Root stack boundary for AKS platform configuration. The lab maps this boundary to the existing `k8s-test-cicd` cluster and does not create another AKS by default.

State key: `platform/aks.tfstate`
