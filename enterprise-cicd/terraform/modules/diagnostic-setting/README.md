# Diagnostic Setting module

Generic diagnostic setting building block. Service products must pass categories or category groups that are explicitly supported by that Azure resource type; the platform does not guess categories at runtime.
