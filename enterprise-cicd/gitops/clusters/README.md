# GitOps Clusters

Cluster bootstrap and Flux registration boundary. Current lab target is the existing `k8s-test-cicd` AKS Automatic cluster; future production maps clusters to independent environment/subscription boundaries.
