# TEST GitOps Environment

Pre-production verification environment. Promotion consumes the same artifact digest produced by CI; environment-specific configuration is reviewed separately from build outputs.
