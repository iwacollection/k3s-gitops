{
  "apiVersion": "platform.github/v1",
  "kind": "EnvironmentPolicy",
  "metadata": {
    "name": "test",
    "environment": "test"
  },
  "spec": {
    "purpose": "OIDC trust boundary for governed TEST read/verification workflows",
    "mustExistBeforeAzureFederation": true,
    "implicitCreationAllowed": false,
    "oidcSubject": "repo:iwacollection/k3s-gitops:environment:test",
    "deploymentBranchPolicy": {
      "mode": "selected-branches",
      "allowed": [
        "design/azure-enterprise-control-plane-v1",
        "main"
      ]
    },
    "environmentSecretsRequired": false,
    "longLivedAzureCredentialsAllowed": false,
    "requiredWorkflowProperties": {
      "idTokenWrite": true,
      "azureClientSecretForbidden": true
    },
    "notes": [
      "The TEST environment is an identity boundary, not a place to store Azure client secrets.",
      "Before PR #5 is merged, the design branch is temporarily allowed for controlled activation and verification.",
      "After the control plane is merged, remove the design branch from the TEST environment policy and retain trusted protected branches only."
    ]
  }
}
