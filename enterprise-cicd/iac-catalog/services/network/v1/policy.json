{
  "dev": {
    "enabled": true,
    "profile": "vnet-subnet-only",
    "paidFeaturesAllowed": false
  },
  "test": {
    "enabled": false,
    "reason": "network/v1 self-service is DEV-only until the production network approval model is activated"
  },
  "prod": {
    "enabled": false,
    "reason": "network/v1 self-service is DEV-only until the production network approval model is activated"
  }
}
