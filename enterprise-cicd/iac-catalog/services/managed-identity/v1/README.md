# Managed Identity v1

Creates a user-assigned managed identity only. Developer requests cannot inject arbitrary federated credential issuers or subjects; federation and RBAC bindings stay platform-owned security operations.
