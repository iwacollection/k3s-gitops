# Load Balancer v1

Creates a Standard Azure Load Balancer foundation with one frontend, backend pool, health probe and rule. Public exposure creates a Standard Public IP; internal exposure requires an existing subnet resource ID.

This catalog is billable and must execute only through the protected Edge Apply capability. NAT rules and outbound rules are intentionally excluded from v1.
