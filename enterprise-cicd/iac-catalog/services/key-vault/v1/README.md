# Key Vault v1

RBAC-authorized Key Vault with purge protection and soft-delete retention. DEV/TEST are enabled. PROD is intentionally blocked in v1 until the platform root stack owns Private Endpoint and Private DNS, preventing an incomplete production networking pattern.
