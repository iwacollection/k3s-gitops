# Production lifecycle protection baseline

# Enable prevent_destroy on critical resources where supported.
# Resource specific lifecycle blocks should be added together with the resource.
# Example:
# lifecycle {
#   prevent_destroy = true
# }
