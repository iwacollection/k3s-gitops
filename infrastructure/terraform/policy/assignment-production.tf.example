# Production policy assignment template
# Enable after validating policy definitions and target scope.

resource "azurerm_resource_group_policy_assignment" "production_governance" {
  name                 = "production-governance"
  resource_group_id    = var.resource_group_id
  policy_definition_id = var.policy_definition_id
}
