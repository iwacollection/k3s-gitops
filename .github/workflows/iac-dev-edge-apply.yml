name: IaC Dev Edge Apply

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

environment: iac-dev-edge-apply

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Terraform placeholder gate
        run: |
          echo "Edge apply workflow ready"
          echo "OIDC environment: iac-dev-edge-apply"
