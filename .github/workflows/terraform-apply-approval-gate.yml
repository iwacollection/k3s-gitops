name: Terraform Apply Approval Gate

on:
  workflow_dispatch:

jobs:
  approval:
    name: Production Apply Approval
    runs-on: ubuntu-latest
    environment:
      name: production

    steps:
      - name: Approval granted
        run: echo "Production Terraform apply approved"
