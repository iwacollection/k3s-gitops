name: IaC Dev Foundation Apply

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

environment: iac-dev-foundation-apply

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Terraform placeholder gate
        run: |
          echo "Foundation apply workflow ready"
          echo "OIDC environment: iac-dev-foundation-apply"
