name: IaC Dev IAM Apply

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

environment: iac-dev-iam-apply

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Terraform placeholder gate
        run: |
          echo "IAM apply workflow ready"
          echo "OIDC environment: iac-dev-iam-apply"
