name: IaC Dev Workload Apply

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

environment: iac-dev-workload-apply

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Terraform placeholder gate
        run: |
          echo "Workload apply workflow ready"
          echo "OIDC environment: iac-dev-workload-apply"
