apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: default

# 如果有需要覆盖的配置，可以在这里添加补丁
# patches:
#   - target:
#       kind: Deployment
#       name: nginx-app
#     patch: |-
#       - op: replace
#         path: /spec/replicas
#         value: 2
