apiVersion: v1
kind: Service
metadata:
  name: enterprise-cmdb
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: enterprise-cmdb
  ports:
    - port: 80
      targetPort: 8000
